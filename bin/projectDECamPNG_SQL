#!/usr/bin/env python

"""
This is the simple call to the project_DECam class inside
projectlib.py to create full focal plane fits files and grayscale PNGs
of DECam exposure images. This version uses SQL to search for the
'red' files.

Felipe Menanteau, May 2014
"""

import time
from qatoolkit import projectlib  as proj

# The start time
t0 = time.time()

# Get the command line options
args    = proj.cmdline()
# Into a dictionary
kwargs = vars(args)
p = proj.project_DECam(args.runID,args.exposureID)
p.run(SQL=True,**kwargs)
print "# Total time: %s" % proj.elapsed_time(t0)


