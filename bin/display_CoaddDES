#!/usr/bin/env python

"""

This is simple method/script to display DECam detections using ds9'
xpa and overlay NOMAD catalog for that section in order the check
astrometry aligment. This one is for coadded MEF fits files and SEx
catalogs.

Felipe Menanteau, NCSA, Sep 2015.

"""

from qatoolkit import display

args = display.cmdline_coadd()
d = display.DisplayDES(**args.__dict__) # Pass it as a dictionary, as required by fuctions/classes

# Display Science
d.showSCI()

# Display Catalog
d.readCat()
d.displayCatalog(imaflags=args.imaflags)

## Display Weight if required
if args.ShowWeight:
    d.showWGT()

